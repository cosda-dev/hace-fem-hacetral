[package]
name = "hace-fem-hacetral"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["alloc", "libm", "std"]
alloc = []
std = []
libm = ["dep:libm"]

[dependencies]
libm = { version = "0.2", optional = true }

[lib]
path = "src/lib.rs"
